## Teoria a aprender
---
* **argc / argv** — cómo recibir argumentos en main ✅
* **std::string** — la clase string de C++ ✅
* **Namespaces** — qué son y cómo usarlos ✅
* **Clases** — estructura básica, .hpp y .cpp ✅
* **Atributos y métodos** — public vs private
* Constructores y destructores
* Initialization lists
* Métodos const
* **Static** — atributos y métodos estáticos
* **std::cout** / `std::cin` / `std::cerr`
* **std::getline** — leer líneas enteras
* **iomanip** — setw, right, left para formatear columnas
* **Include guards** — `#ifndef` / `#define` / `#endif`
* Makefile para C++
* **toupper / cctype** — manipular caracteres
* Arrays estáticos dentro de clases
* **Separación de código** — .hpp vs .cpp